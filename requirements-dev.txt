# eOVPN-Pro Development Dependencies
# وابستگی‌های توسعه eOVPN-Pro
#
# Install with: pip install -r requirements-dev.txt
# نصب با دستور: pip install -r requirements-dev.txt

# Core runtime / زمان اجرا
-r requirements.txt

# Linting, formatting and type checking / لینت، قالب‌بندی و بررسی نوع
# ruff is the single linter/formatter for this project; it fully replaces
# flake8, isort and pycodestyle. Configuration lives in pyproject.toml.
# ruff تنها لینتر و قالب‌بند این پروژه است و جایگزین کامل flake8، isort و
# pycodestyle می‌شود. پیکربندی آن در pyproject.toml قرار دارد.
ruff>=0.5.0
mypy>=1.10.0

# Packaging validation / اعتبارسنجی بسته‌بندی
build>=1.0.0
twine>=5.0.0

# Type stubs / فایل‌های نوع (stub)
types-setuptools>=70.0.0
# Security audit / ممیزی امنیتی وابستگی‌ها
pip-audit>=2.7.0
